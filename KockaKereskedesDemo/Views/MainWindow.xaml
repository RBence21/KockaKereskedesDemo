<Window x:Class="KockaKereskedesDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KockaKereskedesDemo"
        mc:Ignorable="d"
        Title="KockaKereskedés"
        WindowState="Maximized"
        MinHeight="550" MinWidth="900"
        Height="550" Width="900">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="350"/>
        </Grid.ColumnDefinitions>
        <ContentControl Grid.Column="0" Content="{Binding PresentedView}"/>
        <ContentControl Grid.Column="1" Content="{Binding PresentedSideView}"/>
    </Grid>
</Window>
